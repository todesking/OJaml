(* using: Predef *)
package test.ml0

import com.todesking.ojaml.ml0.lib.Predef.+

module Mono {
  let lit_int = 1 ;
  (* lit_int: Int = 1 *)

  let x =
    let f = fun x => x in f 1 ;
  (* x: Int = 1 *)
}
