(* using: Predef *)

package test.ml0

import com.todesking.ojaml.ml0.lib.Predef
import Predef.{Cons, Nil}
import Predef.+

module List_test {
  let ilist = Cons 1 (Cons 2 (Cons 3 Nil)) ;
  (* ilist: List Int = Cons 1 (Cons 2 (Cons 3 Nil)) *)

  let ilist1 = Predef.list_map (fun n => n + 1) ilist ;
  (* ilist1: List Int = Cons 2 (Cons 3 (Cons 4 Nil)) *)
}
